package com.tanmeyah.practice.Entity;

/**
 * Spring Security {@code hasRole("ADMIN")} expects authority {@code ROLE_ADMIN}.
 * Enum names match that convention.
 */
public enum Role {
    ROLE_USER,
    ROLE_ADMIN
}
